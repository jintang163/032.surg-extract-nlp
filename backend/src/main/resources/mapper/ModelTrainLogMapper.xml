<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.surg.extract.mapper.ModelTrainLogMapper">

    <select id="selectLatestSuccess" resultType="com.surg.extract.entity.ModelTrainLog">
        SELECT * FROM model_train_log
        WHERE deleted = 0 AND train_status = 'SUCCESS'
        ORDER BY train_end_time DESC
        LIMIT 1
    </select>

    <select id="selectByDateRange" resultType="com.surg.extract.entity.ModelTrainLog">
        SELECT * FROM model_train_log
        WHERE deleted = 0
        <if test="startDate != null">
            AND DATE(train_start_time) <![CDATA[>=]]> #{startDate}
        </if>
        <if test="endDate != null">
            AND DATE(train_start_time) <![CDATA[<=]]> #{endDate}
        </if>
        ORDER BY train_start_time DESC
    </select>

    <select id="selectLatestN" resultType="com.surg.extract.entity.ModelTrainLog">
        SELECT * FROM model_train_log
        WHERE deleted = 0
        ORDER BY created_time DESC
        LIMIT #{limit}
    </select>

</mapper>
